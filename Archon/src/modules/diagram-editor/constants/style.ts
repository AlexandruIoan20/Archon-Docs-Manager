/** Stroke weights offered by the toolbar. */
export const STROKE_WIDTHS = [1, 1.5, 2, 3] as const

/** Font size range of shapes and free text, in px. */
export const FONT_SIZE = { min: 8, max: 72 } as const
