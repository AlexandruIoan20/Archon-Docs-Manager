/** Below this canvas size the minimap hides (it can be shown on demand). */
export const MINIMAP_MIN = { width: 560, height: 360 }

/** Below this canvas width the hint moves up, clear of the zoom controls. */
export const HINT_RAISE_BELOW = 640
